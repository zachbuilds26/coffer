# Starknet mainnet RPC endpoint used for Coffer’s public transaction reads.
# The default public Lava endpoint is used when this is omitted.
NEXT_PUBLIC_STARKNET_RPC_URL=https://rpc.starknet.lava.build
